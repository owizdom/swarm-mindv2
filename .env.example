# ── LLM Provider — pick one: anthropic, openai, eigenai ──────────
LLM_PROVIDER=anthropic

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-5

# OpenAI
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o

# EigenAI
# EIGENAI_API_KEY=your_key
# EIGENAI_API_URL=https://api.eigenai.xyz/v1
# EIGENAI_MODEL=gpt-oss-120b-f16

# ── NASA ──────────────────────────────────────────────────────────
# Get a free key at https://api.nasa.gov — DEMO_KEY works (30 req/hr)
NASA_API_KEY=DEMO_KEY

# ── EigenDA Proxy ─────────────────────────────────────────────────
# Start with: docker run -p 4242:4242 ghcr.io/layr-labs/eigenda-proxy:latest --memstore.enabled
EIGENDA_PROXY_URL=http://localhost:4242

# ── Swarm Config ──────────────────────────────────────────────────
SYNC_INTERVAL_MS=2000
PHEROMONE_DECAY=0.12
CRITICAL_DENSITY=0.55
TOKEN_BUDGET_PER_AGENT=50000

# ── Dashboard ─────────────────────────────────────────────────────
DASHBOARD_PORT=3000
